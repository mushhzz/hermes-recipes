# Managed by Ansible (playbook.yml) — provisioning/templates/hermes.env.j2
#
# Provider: openai-codex (OAuth via a Codex subscription, not an API key).
# No key belongs in this file for that provider — after the playbook runs,
# SSH in and run `hermes auth` once to complete the interactive OAuth login;
# Ansible cannot drive that browser/device-code flow for you.
#
# If you use a different provider, or add a second one as a fallback, its key
# goes here instead, e.g.:
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=

# Discord bot token, if you use the Discord platform adapter for chat/reports.
# DISCORD_BOT_TOKEN=
# DISCORD_ALLOWED_USERS=
